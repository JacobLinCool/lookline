{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "lookline",
  "account_id": "b1c3d1b89f9131a84a0f1f6a973232f1",
  "main": "vinext/server/fetch-handler",
  "compatibility_date": "2026-09-01",
  "compatibility_flags": ["nodejs_compat"],
  "assets": { "directory": "dist/client", "not_found_handling": "none", "binding": "ASSETS" },
  "observability": { "enabled": true },
  // Image generation runs after the response via `after()` (waitUntil) and needs CPU headroom.
  "limits": { "cpu_ms": 60000 },
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "lookline",
      "database_id": "28602478-a8bc-49fc-a7f2-c295e2c40939",
      "migrations_dir": "../../packages/db/drizzle",
    },
  ],
  "r2_buckets": [{ "binding": "STORAGE", "bucket_name": "lookline-media" }],
  // Non-secret defaults. Secrets (SESSION_SECRET, GEMINI_API_KEY, OPENAI_API_KEY, TYPESAFE_API_KEY)
  // live in `.dev.vars` locally and in `wrangler secret put <NAME>` for production.
  "vars": {
    "TYPESAFE_MODEL": "jev-1.13.0",
    // The escalation path has the text budget's leftovers, and the chain shares one deadline:
    // Luna spends all 3.5 s and never yields to the fallback. Flash-Lite is the one chosen for
    // latency, so it goes first.
    "LLM_TEXT_PROVIDER": "gemini",
    "OPENAI_TEXT_MODEL": "gpt-5.6-luna",
    "OPENAI_IMAGE_MODEL": "gpt-image-2.5-flare",
    "GEMINI_TEXT_MODEL": "gemini-3.5-flash-lite",
    "GEMINI_IMAGE_MODEL": "gemini-3.1-flash-image",
    "LLM_IMAGE_QUALITY": "high",
  },
}
